/*var g = new Graph(6);

var vertices = [new Letter('a', 0, []), new Letter('b', 1, []), new Letter('c', 2, []), new Letter('d', 4, [])];

for (let i = 0; i < vertices.length; i++) {
    g.addVertex(vertices[i])
}

g.addEdge(0, 2)

var grid = ['d', 'r', 't', 'i', 'l', 'a', 'n', 'r', 'r', 's', 'a', 'e', 's', 'f', 'd', 'u']





g.check_connected('ac')*/